import { SpeechButton } from './speech-button';

describe('SpeechButton', () => {
  it('should create an instance', () => {
    const directive = new SpeechButton();
    expect(directive).toBeTruthy();
  });
});

import { ScrollHere } from './scroll-here';

describe('ScrollHere', () => {
  it('should create an instance', () => {
    const directive = new ScrollHere();
    expect(directive).toBeTruthy();
  });
});
